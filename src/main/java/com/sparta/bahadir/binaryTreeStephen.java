package com.sparta.bahadir;

public class binaryTreeStephen {
/*
    static class Node {


        int data;
        Node left;
        Node right;

        //constructor
        Node(int data) {
            this.data = data;
            Node left;
            Node right;
//constructor
            Node( int data){
                this.data = data;
                this.left = null;
                this.right = null;
                1
                eclass btreet
                void add (Node start,int data){
                    if (start.data > data) {
                        if (start.left == nutt) {
                            start.Left = new Node(data);
                        } else
                            add(start.left data);
                        } else if (start.data < data) {
                            if (start.right == null)
                                start.right = new.Node(data);) else{
                                add(start.right, data);
                                void insertLeft (int())
                                public static void main (string(]args)1
                                Node start = new Node(10);


                            }
*/
                        }

